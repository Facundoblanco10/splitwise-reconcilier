splitwise:
  group_id: 12345678
  my_user_id: 111

# One entry per group member. default_entity is used when no [ENTITY:PERSON]
# tag is present in the expense notes.
members:
  - splitwise_id: 111
    default_entity: SCOTIA
  - splitwise_id: 222
    default_entity: SANTANDER
